import type { backendInterface, Product, Category, Cart, Order, OrderStatus, UserRole, StripeSessionStatus, _ImmutableObjectStorageCreateCertificateResult, _ImmutableObjectStorageRefillResult } from "../backend";

const sampleCategories: Category[] = [
  { id: BigInt(1), name: "Electronics", description: "Phones, laptops, and gadgets" },
  { id: BigInt(2), name: "Clothing", description: "Apparel and accessories" },
  { id: BigInt(3), name: "Home & Garden", description: "Furniture, decor, and tools" },
  { id: BigInt(4), name: "Sports", description: "Fitness and outdoor gear" },
];

const sampleProducts: Product[] = [
  {
    id: BigInt(1),
    name: "Wireless Noise-Cancelling Headphones",
    description: "Premium over-ear headphones with active noise cancellation, 30-hour battery life, and exceptional audio quality.",
    price: BigInt(29999),
    stock: BigInt(42),
    category: "Electronics",
    isActive: true,
    createdAt: BigInt(Date.now() * 1_000_000),
  },
  {
    id: BigInt(2),
    name: "Minimalist Leather Watch",
    description: "Elegant timepiece with a genuine leather strap and sapphire crystal glass. Water-resistant to 50m.",
    price: BigInt(18900),
    stock: BigInt(15),
    category: "Clothing",
    isActive: true,
    createdAt: BigInt(Date.now() * 1_000_000),
  },
  {
    id: BigInt(3),
    name: "Ergonomic Office Chair",
    description: "Fully adjustable lumbar support, breathable mesh back, and smooth-rolling casters. Built for all-day comfort.",
    price: BigInt(45000),
    stock: BigInt(8),
    category: "Home & Garden",
    isActive: true,
    createdAt: BigInt(Date.now() * 1_000_000),
  },
  {
    id: BigInt(4),
    name: "Ultra-Light Running Shoes",
    description: "Responsive cushioning and breathable upper mesh. Designed for long-distance runners seeking speed and comfort.",
    price: BigInt(12900),
    stock: BigInt(30),
    category: "Sports",
    isActive: true,
    createdAt: BigInt(Date.now() * 1_000_000),
  },
  {
    id: BigInt(5),
    name: "Stainless Steel Water Bottle",
    description: "Double-walled vacuum insulation keeps drinks cold 24h or hot 12h. BPA-free, 32oz capacity.",
    price: BigInt(3500),
    stock: BigInt(100),
    category: "Sports",
    isActive: true,
    createdAt: BigInt(Date.now() * 1_000_000),
  },
  {
    id: BigInt(6),
    name: "Bamboo Cutting Board Set",
    description: "Set of 3 eco-friendly bamboo cutting boards with juice grooves. Naturally antimicrobial.",
    price: BigInt(2800),
    stock: BigInt(50),
    category: "Home & Garden",
    isActive: true,
    createdAt: BigInt(Date.now() * 1_000_000),
  },
];

const sampleOrders: Order[] = [
  {
    id: BigInt(1001),
    status: "pending" as unknown as OrderStatus,
    total: BigInt(29999),
    createdAt: BigInt((Date.now() - 86400000) * 1_000_000),
    customerEmail: "customer@example.com",
    customerId: { toText: () => "user-principal-1" } as any,
    shippingAddress: {
      fullName: "Alex Johnson",
      addressLine1: "123 Main Street",
      addressLine2: "Apt 4B",
      city: "New York",
      state: "NY",
      postalCode: "10001",
      country: "US",
    },
    items: [
      { productId: BigInt(1), name: "Wireless Noise-Cancelling Headphones", quantity: BigInt(1), price: BigInt(29999) },
    ],
  },
  {
    id: BigInt(1002),
    status: "shipped" as unknown as OrderStatus,
    total: BigInt(31800),
    createdAt: BigInt((Date.now() - 172800000) * 1_000_000),
    customerEmail: "customer@example.com",
    customerId: { toText: () => "user-principal-1" } as any,
    shippingAddress: {
      fullName: "Alex Johnson",
      addressLine1: "123 Main Street",
      addressLine2: "",
      city: "New York",
      state: "NY",
      postalCode: "10001",
      country: "US",
    },
    items: [
      { productId: BigInt(2), name: "Minimalist Leather Watch", quantity: BigInt(1), price: BigInt(18900) },
      { productId: BigInt(5), name: "Stainless Steel Water Bottle", quantity: BigInt(2), price: BigInt(6200) },
    ],
  },
  {
    id: BigInt(1003),
    status: "delivered" as unknown as OrderStatus,
    total: BigInt(45000),
    createdAt: BigInt((Date.now() - 604800000) * 1_000_000),
    customerEmail: "customer@example.com",
    customerId: { toText: () => "user-principal-1" } as any,
    shippingAddress: {
      fullName: "Alex Johnson",
      addressLine1: "456 Park Ave",
      addressLine2: "",
      city: "Brooklyn",
      state: "NY",
      postalCode: "11201",
      country: "US",
    },
    items: [
      { productId: BigInt(3), name: "Ergonomic Office Chair", quantity: BigInt(1), price: BigInt(45000) },
    ],
  },
];

export const mockBackend: backendInterface = {
  addAdmin: async () => undefined,
  addToCart: async () => undefined,
  assignCallerUserRole: async () => undefined,
  clearCart: async () => undefined,
  createCategory: async (input) => ({ id: BigInt(5), name: input.name, description: input.description }),
  createCheckoutSession: async () => "mock-session-id",
  createOrder: async (input) => ({
    id: BigInt(2000),
    status: "pending" as unknown as OrderStatus,
    total: input.total,
    createdAt: BigInt(Date.now() * 1_000_000),
    shippingAddress: input.shippingAddress,
    customerId: { toText: () => "user-principal-1" } as any,
    items: input.items,
    customerEmail: input.customerEmail,
  }),
  createProduct: async (input) => ({
    id: BigInt(99),
    name: input.name,
    description: input.description,
    price: input.price,
    stock: input.stock,
    category: input.category,
    isActive: input.isActive,
    createdAt: BigInt(Date.now() * 1_000_000),
  }),
  deleteProduct: async () => undefined,
  getAllOrders: async () => sampleOrders,
  getCallerUserRole: async () => "admin" as unknown as UserRole,
  getCart: async (): Promise<Cart> => ({
    items: [
      { productId: BigInt(1), name: "Wireless Noise-Cancelling Headphones", quantity: BigInt(1), price: BigInt(29999) },
      { productId: BigInt(4), name: "Ultra-Light Running Shoes", quantity: BigInt(2), price: BigInt(25800) },
    ],
  }),
  getCategories: async () => sampleCategories,
  getCustomerOrders: async () => sampleOrders,
  getOrder: async (id) => sampleOrders.find((o) => o.id === id) || null,
  getProductById: async (id) => sampleProducts.find((p) => p.id === id) || null,
  getProducts: async () => sampleProducts,
  getProductsByCategory: async (category) => sampleProducts.filter((p) => p.category === category),
  getStats: async () => ({ totalOrders: BigInt(3), totalRevenue: BigInt(106799) }),
  getStripeSessionStatus: async () => ({
    __kind__: "completed",
    completed: { userPrincipal: "user-principal-1", response: "success" },
  } as StripeSessionStatus),
  isAdmin: async () => true,
  isCallerAdmin: async () => true,
  isStripeConfigured: async () => false,
  removeAdmin: async () => undefined,
  removeFromCart: async () => undefined,
  searchProducts: async (term) =>
    sampleProducts.filter((p) => p.name.toLowerCase().includes(term.toLowerCase())),
  setStripeConfiguration: async () => undefined,
  transform: async (input) => ({ status: BigInt(200), body: input.response.body, headers: input.response.headers }),
  updateCartItem: async () => undefined,
  updateOrderStatus: async (id, status) => {
    const order = sampleOrders.find((o) => o.id === id);
    return order ? { ...order, status } : null;
  },
  updateProduct: async (id, input) => {
    const product = sampleProducts.find((p) => p.id === id);
    return product ? { ...product, ...input } : null;
  },
  _immutableObjectStorageBlobsAreLive: async (hashes) => hashes.map(() => true),
  _immutableObjectStorageBlobsToDelete: async () => [],
  _immutableObjectStorageConfirmBlobDeletion: async () => undefined,
  _immutableObjectStorageCreateCertificate: async (): Promise<_ImmutableObjectStorageCreateCertificateResult> => ({ method: "GET", blob_hash: "" }),
  _immutableObjectStorageRefillCashier: async (): Promise<_ImmutableObjectStorageRefillResult> => ({ success: true, topped_up_amount: BigInt(0) }),
  _immutableObjectStorageUpdateGatewayPrincipals: async () => undefined,
  _initializeAccessControl: async () => undefined,
};
